# Python 3.11.3
# Binance & Crypto APIs
ccxt==4.5.2
python-binance==1.0.29

# Technical indicators
# pandas_ta 0.3.14b0 was REMOVED from PyPI and its upstream GitHub repo is gone,
# so the pinned version is no longer installable. It is vendored under
# vendor/pandas-ta/ (the `from numpy import NaN` refs patched for numpy>=2).
# Installs as the importable `pandas_ta` package. Requires numpy<2 + pandas<3
# (pinned below) for full compatibility.
./vendor/pandas-ta
ta-lib==0.6.6

# Web app / API (optional for orchestration)
fastapi==0.116.1
uvicorn==0.35.0

# Date & time utilities
python-dateutil==2.8.2

# Logging & config
python-dotenv==1.0.0

# Backtesting (optional but useful for validation)
backtrader==1.9.78.123

# Core data handling
# pandas==2.2.2
# numpy==1.26.4 #python 3.12.3

pandas==2.0.3
numpy==1.25.0
requests==2.31.0
beautifulsoup4==4.12.2
newspaper3k==0.2.8
python-dateutil==2.8.2

# Chart pattern recognition
# candle-analyzer
# streaming-indicators


# AI & NLP
openai==1.102.0
transformers==4.55.4
sentence-transformers==5.1.0

# Vector database
pinecone-client==6.0.0

# Reinforcement Learning
stable-baselines3==2.7.0
gymnasium==1.1.1
ray[rllib]==2.49.0

# Visualization (optional but helpful)
matplotlib==3.8.2
plotly==6.3.0

langchain==0.3.27
langgraph==0.4.3
pydantic==2.11.7

telegram==0.0.1
python-telegram-bot==22.3