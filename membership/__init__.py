"""Membership / subscriptions (Bot D): paid access to the signals channel and
the Pro control bot. Fully flag-gated by config.MEMBERSHIP_ENABLED — when off,
nothing in this package is imported by the runtime."""
